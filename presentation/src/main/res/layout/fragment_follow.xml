<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar_follow"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginVertical="20dp"
            android:background="@color/velog_white"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar_follow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:title="Follow"
                app:titleMarginStart="20dp"
                app:titleTextAppearance="@style/TextAppearance.VelogM.Display"
                app:titleTextColor="@color/velog_gray700">

                <ImageView
                    android:id="@+id/iv_follow_reading_glasses"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:layout_marginEnd="20dp"
                    android:contentDescription="@string/content_description_follow_reading_glasses"
                    android:padding="2dp"
                    android:src="@drawable/ic_follow_reading_glasses" />

            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <TextView
            android:id="@+id/tv_follow_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="24dp"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:text="@string/tv_follow_title"
            android:textAppearance="@style/TextAppearance.VelogM.Body2_B"
            android:textColor="@color/velog_gray700"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/appbar_follow" />

        <View
            android:id="@+id/view_follow_main_line"
            android:layout_width="0dp"
            android:layout_height="3dp"
            android:background="@color/main"
            app:layout_constraintEnd_toEndOf="@id/tv_follow_title"
            app:layout_constraintStart_toStartOf="@id/tv_follow_title"
            app:layout_constraintTop_toBottomOf="@id/tv_follow_title" />

        <View
            android:id="@+id/view_follow_line"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/velog_gray200"
            app:layout_constraintTop_toBottomOf="@id/view_follow_main_line" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_follow"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="32dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/view_follow_line"
            tools:listitem="@layout/item_rv_follow" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>